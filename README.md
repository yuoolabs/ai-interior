# AI 装修项目（启动说明）

## 快速预览
在项目根目录执行：

```bash
npm run dev
```

启动后访问：

`http://127.0.0.1:8001`

## 常用命令
- `npm run dev`：启动开发预览（转发到 `demo`）
- `npm run test`：运行测试（转发到 `demo`）
- `npm run preflight`：运行预检（转发到 `demo`）

## 目录说明
- `demo/`：主应用代码与详细文档
